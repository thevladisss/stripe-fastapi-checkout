# Let Nixpacks auto-detect Python from requirements.txt
# Just add Node.js for building the frontend

[phases.setup]
nixPkgs = ["nodejs"]

[phases.build]
cmds = ["cd ui && npm install && npm run build"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
